# docker-compose.yml
version: '3.8'
services:
  nats:
    image: nats:latest
    command: "-js -sd /data -m 8222"
    ports:
      - "4222:4222"   # client
      - "8222:8222"   # HTTP monitor
    volumes:
      - nats_data:/data

volumes:
  nats_data:

