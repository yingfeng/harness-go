package deep

const (
	TaskPrompt   = "Each sub-task should be specific, actionable, ordered logically, and verified after completion."
	VerifyPrompt = "After completing each sub-task, verify the output is correct and the objective was met."
)
